# Template for the admin panel's production env.
# Copy to `.env.production` and edit values before `npm run build`.

VITE_API_ORIGIN=https://api.<your-root-domain>
VITE_PUBLIC_SITE_URL=https://<your-root-domain>

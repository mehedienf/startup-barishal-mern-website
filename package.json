{
  "name": "startup-barishal-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Startup Barishal monorepo: client (public site), admin (console), and server (Express API).",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client && npm install --prefix admin",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:admin": "npm run dev --prefix admin",
    "dev": "node scripts/dev-all.js",
    "build:client": "npm run build --prefix client",
    "build:admin": "npm run build --prefix admin",
    "build": "npm run build:client && npm run build:admin",
    "start:server": "npm start --prefix server"
  },
  "devDependencies": {
    "npm-run-all2": "^8.0.4"
  },
  "type": "module"
}
